# BACKLOG - flashsieve
src/incremental_watch.rs:197 | silent-fallback | low | Recursive walk errors are silently swallowed via 'let _' | FIXED (Law 10, same recall class as the done :150): `let _ = walk_dir_inner(..)` -> `walk_dir_inner(..)?`. A permission-denied/unreadable subdirectory was silently skipped, so every file beneath it was silently absent from the index (invisible recall loss). Now propagates the io::Error (fail-closed), consistent with index_changes. | status=done
src/incremental_watch.rs:150 | silent-fallback | high | File read failures are silently swallowed via 'if let Ok(data) = std::fs::read(path)' | Propagate the file read error | status=done | FIXED (Law-10 recall bug): index_changes now does std::fs::read(path).map_err(|source| Error::Io{path,source})? instead of `if let Ok(data)` which silently skipped unreadable modified files (their content never indexed -> silently missing from later queries). Added Error::Io{path,source} variant (non_exhaustive enum, thiserror #[source]). Matches the doc contract 'Returns an error if file reading fails'. Proving test index_changes_propagates_read_error_instead_of_skipping asserts Error::Io with the path for a missing file.
src/incremental_watch.rs:199 | silent-fallback | low | Metadata read failures are silently ignored via 'if let Ok(meta)' | FIXED (Law 10, same edit): `if let Ok(meta) = entry.metadata()` -> `let meta = entry.metadata()?`. An unreadable file's metadata failure no longer silently drops it from indexing; the io::Error propagates. | status=done
src/incremental.rs:330 | bug | high | self.last_byte is not updated in remove_blocks, leaving it stale for subsequent appends | FIXED: set self.last_byte=None after suffix-only removal (final block always dropped, so cached boundary byte now points at removed data; cannot recover new boundary from aggregated blooms so omit the cross-boundary n-gram rather than pair with a stale byte). Proving test tests/unit/incremental.rs::remove_then_append_does_not_insert_stale_boundary_ngram (exact-pair table is deterministic: asserts ('X','Z') stale n-gram absent + ('Z','c') own n-gram present). unit_incremental 5 green. | status=done
src/mmap_query.rs:285 | bug | high | Deprecated bloom() fallback returns NgramBloomRef with empty bloom_data and 0 bits, causing panics | FIXED (better than the dummy-slice suggestion): guard `maybe_contains_bloom` to return false when num_bits==0 (an empty bloom contains nothing) - covers maybe_contains_any/all too. Root cause was mask=u64::MAX -> huge index -> OOB read of empty bloom_data. Proving test tests/unit/mmap_index.rs::deprecated_bloom_out_of_range_is_safe_not_panic. lib 60 + mmap_index 3 green. | status=done
src/transport.rs:271 | dedup | medium | Slow bit-serial crc32_simple duplicated from codec.rs | FIXED (ONE-PLACE): deleted transport's bit-serial copy; made codec::crc32_simple pub(crate), re-exported it via index/mod.rs, and transport now `use crate::index::crc32_simple`. Verified byte-identical output (both IEEE 0xEDB88320, init 0xFFFFFFFF, final !crc; the table is the mathematically-equivalent 8-bit-unrolled form) so all 6 transport call sites get the fast path with no behavior change. | status=done
src/transport.rs:172 | capability | medium | rle_decompress does not implement 0xFE literal runs specified in the documentation | FIXED (coherence: code now matches the documented wire format): added the `0xFE count byte...` literal-run branch to rle_decompress (reads count, copies that many raw bytes, with truncation + overflow + expected-size guards). Safe with existing data because rle_compress reserves 0xFE (escapes every raw 0xFE via a 0xFF run), so a bare 0xFE at a marker position is ALWAYS a literal-run header and never collides. This makes the decoder spec-complete/forward-compatible. Proving tests rle_decompress_decodes_0xfe_literal_run ([0xFE,3,a,b,c]->abc) + rle_decompress_rejects_truncated_0xfe_literal_run. Existing 0xFE/0xFF round-trip tests still pass (compress emits 0xFE only inside 0xFF runs). | status=done
src/index/codec.rs:141 | capability | low | from_bytes_checked does not validate that all blocks share the same bloom filter bit count | FIXED: from_bytes_checked now tracks the first block's num_bits and returns Error::IncompatibleIndexConfiguration (reused existing variant, ONE-PLACE) if any later block's num_bits differs - a per-index invariant (all blocks share one bloom size) that a corrupt/hand-crafted blob could violate, producing an index whose candidate_blocks/append paths assume uniform sizing. Proving test from_bytes_checked_rejects_mismatched_block_bloom_bits (BlockIndex::new with 1024- and 2048-bit blooms -> to_bytes -> from_bytes_checked errors). | status=done
src/index/mod.rs:112 | capability | medium | Empty BlockIndex has bloom_bits set to 0, preventing future block appends | FIXED: new_with_last_byte gained a configured_bloom_bits param - it prefers the caller's configured bit count and only falls back to deriving from blooms.first() when 0 is passed. Both builder call sites (141, 208) pass self.bloom_bits, so an EMPTY index (build(b"")) now retains e.g. 4096 instead of collapsing to 0; the public BlockIndex::new passes 0 (unchanged derive-from-blooms behavior, non-breaking). Added a public configured_bloom_bits() accessor (named to avoid clashing with the crate-internal append resolver `bloom_bits(&self)->Result<usize>` in incremental.rs:347, which is the exact method that used to return Err(ZeroBloomBits) for an empty index and now returns Ok(config)). Proving doctest: BlockIndexBuilder::new().bloom_bits(4096).build(b"") -> configured_bloom_bits()==4096. | status=done
src/bloom/serde.rs:61 | bug | low | from_raw_parts allows input bits vector to be larger than required_words without truncation | FIXED: from_raw_parts now `bits.truncate(required_words)` after the too-small check. ALSO fixed the identical bug in the sibling from_serialized_parts (ONE behavior for both reconstruction entry points) - the row only named from_raw_parts but from_serialized_parts had the same surplus-accept. Surplus words are unaddressable (bit_index_mask caps at num_bits) and would break round-trip identity. Proving tests (truncation_tests): from_raw_parts_truncates_surplus_words (64 bits, 5 words -> 1), from_serialized_parts_truncates_surplus_words (128 bits, 9 words -> 2), from_raw_parts_still_rejects_too_few_words. | status=done
tests/campaign_s_proptest_03.rs:26 | test-gap | low | Unused variable warning in macro flash_cases on $size | FIXED: added #[allow(unused_variables)] to the macro-generated test fn. Underscore-prefixing (the row's suggestion) is WRONG here - $size is shared, and cases that DO use it (NgramBloom::new(size)) would break; the allow lets the block-only cases ignore it cleanly. | status=done
src/incremental_watch.rs:105 | flashsieve | silent-fallback | high | REPOPULATE 2026-07-16: walk_dir errors are silently swallowed during watcher polling, causing all known files to be cleared and falsely flagged as removed, and subsequently re-indexed as new/modified on the next successful poll. | FIXED 2026-07-17: poll() now returns Result<ChangeSet> and maps a failed walk to Error::Io{path,source} before touching state, so a transient walk error can no longer masquerade as a mass deletion or overwrite known_files with the empty set. In-crate callers (unit + adversarial tests) updated; the concurrent poller tolerates legitimate mid-walk Io errors via map_or(0). Proving test poll_on_unwalkable_root_fails_loud_and_preserves_state (register a file, delete the root, assert Error::Io) + audit_break_it asserts poll on a deleted root is_err(). `cargo test -p flashsieve incremental_watch` + `--test adversarial` green. | status=done
DONE 2026-07-17 | src/mmap_query.rs:248 | flashsieve | silent-fallback | low | REPOPULATE 2026-07-16: Deprecated MmapBlockIndex::histogram silently falls back to a dummy all-zero ByteHistogramRef when try_histogram fails on out of bounds block_id. | FIXED: `histogram()` now `unwrap_or_else(|e| panic!(...))` on the try_histogram Result, failing closed with a message naming the block_id + pointing at try_histogram. The dummy all-zero histogram (which read downstream as "block has no bytes") is gone. Matches the method's own deprecation note ("use try_histogram to avoid panics"). Proving test `deprecated_histogram_out_of_range_fails_closed` (#[should_panic]) + positive `deprecated_accessors_return_real_data_for_a_valid_block`. green exit 0. | status=done
DONE 2026-07-17 | src/mmap_query.rs:285 | flashsieve | silent-fallback | low | REPOPULATE 2026-07-16: Deprecated MmapBlockIndex::bloom silently falls back to a dummy empty NgramBloomRef when try_bloom fails on out of bounds block_id. | FIXED: `bloom()` now fails closed via `unwrap_or_else(|e| panic!(...))` instead of a dummy empty bloom (which answered "contains nothing" to every query, silently dropping recall). Proving test `deprecated_bloom_out_of_range_fails_closed` (#[should_panic]); `try_bloom_and_try_histogram_return_err_out_of_range_and_ok_in_range` pins the fallible variants. Replaced the old test `deprecated_bloom_out_of_range_is_safe_not_panic` which had encoded the silent fallback as the contract. green exit 0. | status=done
src/bloom/query.rs:251-259 + :293-301 | flashsieve | dedup | medium | MANUAL 2026-07-17 (ONE-PLACE, latent recall bug): the blocked-bloom 3-probe bit-test `bit_index = h1 + h2*probe + probe*0x9E3779B97F4A7C15 & 511; block[bit>>6] & (1<<(bit&63))` is copy-pasted VERBATIM between `BlockedNgramBloom::maybe_contains` (251-259) and `BlockedNgramBloom::maybe_contains_all` (293-301). This probe formula must ALSO stay byte-identical to the one `insert` uses (filter.rs) or the bloom gets false negatives (recall loss on the scanner hot path). Three copies of the same index derivation that can silently drift; a single-query vs batch-query disagreement would be an invisible recall bug. | FIXED: Extracted probe-to-bit-index derivation into ONE-PLACE `blocked_probe_bits` / `blocked_probe_bit` in `hash.rs` used by `BlockedNgramBloom::insert`, `maybe_contains`, and `maybe_contains_all`. Deleted inline duplicate probe loops across all 3 sites. Proving tests: `blocked_bloom_differential_query_matches_batch_and_flat` + proptest `blocked_bloom_differential_query_and_flat_equivalence`. cargo test 948 green. | status=done
src/bloom/query.rs:76-83 + :251 + :293 + :232 | flashsieve | coherence | medium | MANUAL 2026-07-17 (ONE-PLACE): the number of hash probes is the literal `3` hardcoded in THREE query hot paths — `maybe_contains_bloom` (unrolled idx0/idx1/idx2, 80-83, comment "Unroll k=3"), blocked `maybe_contains` (`0..3u64`, 251), blocked `maybe_contains_all` (`0..3u64`, 293) — while the named constant `NUM_HASHES` is used ONLY by `estimated_false_positive_rate` (232, `fill_ratio.powf(NUM_HASHES)`). Nothing links them: if `NUM_HASHES` is ever changed to 4, the FPR estimate updates but every query still probes exactly 3 bits, so the estimate silently describes a filter that no longer exists, and (depending on insert) recall/precision drift. | FIXED: `blocked_probe_bits` bounds probe iterations directly by `0..u64::from(NUM_HASHES)`. Unrolled probe calculations in `NgramBloom::maybe_contains_bloom`, `NgramBloom::insert_ngram`, and `NgramBloomRef::maybe_contains_bloom` use `flat_probe_bit` and are compile-tied to `NUM_HASHES` via `const _: () = assert!(NUM_HASHES == 3);`. Single owner for FPR exponent, query probes, and insert probes. cargo test 948 green. | status=done
src/bloom/query.rs:240-303 | flashsieve | test-gap | medium | MANUAL 2026-07-17: the blocked-bloom tests (`blocked_bloom_has_zero_false_negatives_and_low_fp_rate`, 470) exercise `maybe_contains` and (implicitly) insert, but NOTHING asserts that `maybe_contains(a,b)` and `maybe_contains_all(&[(a,b)])` return the SAME answer — which is exactly the invariant threatened by the duplicated probe formula (row above). A drift between the two copies would pass every current test yet silently make batch queries disagree with point queries. | FIXED: Added differential unit test `blocked_bloom_differential_query_matches_batch_and_flat` in `query.rs` and differential property test `blocked_bloom_differential_query_and_flat_equivalence` in `proptest_invariants.rs`. Asserts concrete equality `maybe_contains(a,b) == maybe_contains_all(&[(a,b)])` across point/batch/multi-pair queries and verifies exact-pairs equivalence against `NgramBloom`. cargo test 948 green. | status=done
